# Updates

## Missing

- [ ] Folder management: no way in the UI to add, rename, remove
- [ ] Feed management: once a feed has been added, no editing is built--rename, move to folder, delete
- [ ] Starring: We show starred items, but there's no way to star or unstar items.

## Styling updates

- [ ] I want to switch to using DM Sans for our sans serif font. I think we can install via font-source.
- [ ] all/unread toggle has messed up spacing. Looks like maybe none? Spacing for ALL BUTTONS and UI elements should only ever use the mantine built-in size tokens. Do not manually write css for sizing, paddings, margins when we can use the mantine design tokens. This is a key way to maintain consistency. Use the same sizing for the all/unread toggle as we use for the mark all read button. They should match visually.
- [ ] Streams heading in the sidebar appears to have correct spacing. The top "TODAY 30" heading in the main listing does not visually align with the STREAMS. It's tigher to the top than streams. The spacing on each of these needs to align because the unscrolled main window should match the sidebar spacing.
- [ ] Increase article headline size in the listing. Looks too small.

## 3

- Add folder button to match add feed
- Star should be yellow
- list density setting--does it do anything?
- Sparkle RSS links to today (configurable in settings?)
- Change feed "rename" to "edit", show read-only feed url with copy.
- Still not showing favicons. Are they actually available? Should show small before each feed.

Bigger stuff
- Theme picker
- three column.. so list while reading, optionally
- 